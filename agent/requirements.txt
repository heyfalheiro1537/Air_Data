fastapi
uvicorn[standard]

# LangChain + community integrations (Ollama, etc.)
langchain
langchain-community

# Vector store
langchain-chroma
chromadb
tqdm

# Embeddings/Docs utils
PyMuPDF

# RDF / SPARQL
rdflib

# Config / HTTP
python-dotenv
requests

# Pydantic v2 (FastAPI uses v2)
pydantic>=2
